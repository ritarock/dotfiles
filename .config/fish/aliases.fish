alias ltr 'ls -ltr'
alias tree 'tree -NC'
alias gst 'git status'
alias gdf 'git diff --color-words'
alias gcm 'git checkout master'
alias dpurge 'docker stop (docker ps -q) && docker rm (docker ps -q -a)'
alias hub 'hub checkout'
